# --- Google ADK / Gemini / Vertex AI ---
google-cloud-aiplatform[agent_engines,adk]>=1.101.0
google-genai>=1.0.0

# --- ClickHouse (partner) ---
clickhouse-connect>=0.8.16
# Official ClickHouse MCP server (hackathon track requirement). Preinstalled so
# Cloud Run does not depend on a first-request `uvx` download.
mcp-clickhouse>=0.5.0

# --- MCP (ADK <-> mcp-clickhouse server). Pin <2: ADK 2.8.0 expects the 1.x layout ---
mcp>=1.8,<2

# --- API server ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.7.0
pydantic-settings>=2.3.0

# --- Media / utilities ---
pillow>=10.3.0
python-dotenv>=1.0.1
